+------------------+
|   INSTRUCTIONS   |
+------------------+

1) Open a Terminal on the project's root and install
   dependencies with Makefile:

   $ make install


2) To create an executable, use:

   $ make


3) To create and run tests, use:

   $ make test
