#include "ship.hpp"

/*-----------------------------------------------------------*/

Ship::Ship(int vx)
{
    this->vx = vx;
    this->x = 0;
}

/*-----------------------------------------------------------*/

void Ship::setPosition(int x)
{
    this->x = x;
}

/*-----------------------------------------------------------*/

int Ship::getPosition()
{
    return x;
}
